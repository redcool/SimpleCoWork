import { buildControlProposal, validateControlProposal, executableActions } from "./control.js";
export class StudioControlLoop {
  constructor({observer,planner,executor=null,eventLog=null,actor}){this.observer=observer;this.planner=planner;this.executor=executor;this.events=eventLog;this.actor=actor;}
  async round(context={}){if(typeof this.observer!=="function"||typeof this.planner!=="function")throw new Error("ControlLoop 需要 observer 和 planner");const snapshot=await this.observer(context);const raw=await this.planner({snapshot,context,actor:this.actor});const proposal=raw?.protocol?raw:buildControlProposal(raw);const validation=validateControlProposal(proposal,context);if(!validation.ok){this.events?.append("control.proposal.rejected",{actorId:this.actor?.id,errors:validation.errors});return {snapshot,proposal,validation,executed:[]};}const executable=executableActions(proposal);const executed=[];for(const action of executable){if(this.executor)executed.push(await this.executor(action,context));}this.events?.append("control.round.completed",{actorId:this.actor?.id,actionCount:executed.length,decisionCount:proposal.requiresUserDecision?.length||0});return {snapshot,proposal,validation,executed};}
}
