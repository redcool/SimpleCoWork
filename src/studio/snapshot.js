export function buildStudioSnapshot({versionId,versions,stages,tasks,bugs=null,reviews=null,pendingApprovals=null}={}){const version=versions?.get(versionId);const stageRows=stages?.list(versionId)||[];const taskRows=tasks?.list(versionId)||[];const bugRows=bugs?.list?bugs.list(versionId):[];const reviewRows=reviews?.listForVersion?reviews.listForVersion(versionId):[];return {protocol:1,version:{id:version?.id,version:version?.version,status:version?.status},stages:stageRows.map(s=>({key:s.stage_key,status:s.status})),tasks:taskRows.map(t=>({id:t.id,stageKey:t.stageKey,status:t.status,agentRole:t.agentRole,attempt:t.attempt,maxAttempts:t.max_attempts})),bugs:bugRows.map(b=>({id:b.id,status:b.status,severity:b.severity,priority:b.priority,title:b.title})),reviews:reviewRows,pendingApprovals:pendingApprovals||[],generatedAt:new Date().toISOString()};}
export function summarizeSnapshot(snapshot){return {version:snapshot.version,stageCounts:Object.fromEntries(snapshot.stages.map(s=>[s.key,s.status])),taskCounts:snapshot.tasks.reduce((a,t)=>(a[t.status]=(a[t.status]||0)+1,a),{}),openBugs:snapshot.bugs.filter(b=>!["closed","wont-fix"].includes(b.status)).length,approvalRequests:snapshot.pendingApprovals.length};}
